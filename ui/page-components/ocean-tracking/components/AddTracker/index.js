const { Modal } = require('@cogoport/components');

function AddTrackerModal({ show, overflow = 'auto' }) {
	return (
		<div>
			  <Modal size="md" show={show} placement="center">
				hgdhdbjhdgdgd
			</Modal>
		</div>
	);
}
export default AddTrackerModal;
